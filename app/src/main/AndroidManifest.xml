<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.photoglyde.justincornelius.photoglyde">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:name=".Data.Configuration"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".Networking.NoConnection"
                  android:theme="@style/AppTheme"
        >

        </activity>
        <activity
                android:name=".Register"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar"
                android:windowSoftInputMode="adjustPan">
        </activity>
        <activity android:name=".Camera.FinalPost">
        </activity>
        <activity android:name=".Camera.PhotoEditor">
        </activity>
        <activity
                android:name=".MainActivity"
                android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".ExapandDetailActivity"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity> <!-- <activity -->
        <!-- android:name=".Sam" -->
        <!-- android:label="@string/title_activity_sample_tab" -->
        <!-- android:theme="@style/AppTheme"> -->
        <!-- </activity> -->
        <activity
                android:name=".Camera.CamerOpenActivity"
                android:label="@string/title_activity_sample_tab"
                android:theme="@style/AppTheme">
        </activity> <!-- <activity -->
        <!-- android:name=".Main" -->
        <!-- android:label="@string/title_activity_main2" -->
        <!-- android:theme="@style/AppTheme"> -->
        <!-- </activity> -->
        <activity
                android:name=".OnBoardingActivity"
                android:label="@string/title_activity_on_boarding"
                android:theme="@style/AppTheme"
                android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
                android:name=".RegisterPage"
                android:windowSoftInputMode="adjustPan">
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.maps.v2.API_KEY"
                android:value="@string/google_maps_key"/>
        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts"/>

        <activity
                android:name=".DeleveryNow"
                android:label="DeleveryNow">
        </activity>
        <activity
                android:name=".Adapters.RetroFit"
                android:label="@string/title_activity_retro_fit"
                android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Web.NewsWebView">
        </activity>
        <activity
                android:name=".Camera.EditPhoto"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>
        <activity
                android:name=".VideoPlayback.VideoActivity"
                android:label="@string/title_activity_video"
                android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>